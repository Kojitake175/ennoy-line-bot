name: Scheduled ENNOY Scraping

on:
  schedule:
    - cron: '*/5 * * * *' # 5分ごとに実行
  workflow_dispatch:

jobs:
  scrape:
    runs-on: ubuntu-latest
    env:
      LINE_ACCESS_TOKEN: ${{ secrets.LINE_ACCESS_TOKEN }}
      LINE_USER_ID: ${{ secrets.LINE_USER_ID }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install dependencies
        run: npm install

      - name: Restore service account credentials
        run: echo "${{ secrets.GCP_CREDENTIALS_JSON }}" > voltaic-mode-428705-s2-f2f0dcddfa23.json

      - name: Run scraper
        run: node index.js
